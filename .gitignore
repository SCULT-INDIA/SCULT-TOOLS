node_modules/
.next/
out/
build/
coverage/
.env*
!.env.example
*.tsbuildinfo
next-env.d.ts
.DS_Store
.playwright-mcp/

.vercel
.env*

# scult-cli build output (built on publish via prepublishOnly)
cli/dist/

# Local-only DB backups (e.g. the 117MB archive of the 40,456 skill rows deleted 2026-09-16) — never commit.
supabase/backups/
